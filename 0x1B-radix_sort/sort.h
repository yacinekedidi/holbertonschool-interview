#ifndef _SORT_
#define _SORT_
#include <stdio.h>
#include <stdlib.h>


void print_array(const int *, size_t);
void radix_sort(int *, size_t);
int get_digit_count(int);
int get_digit_at(int, int);
int get_max_digit(int *, size_t);
int _pow(int, int);

#endif
